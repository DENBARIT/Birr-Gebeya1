org.gradle.jvmargs=-Xmx4G -XX:MaxMetaspaceSize=2G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
# Project path contains non-ASCII characters (ብར Gebeya) which Gradle's Android
# plugin rejects by default on Windows. This flag disables that safety check so
# the build succeeds in-place without moving the project to a different folder.
android.overridePathCheck=true
